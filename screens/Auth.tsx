import React from 'react';
import Landing from './Landing';

const Auth: React.FC = () => {
    // In this app, Landing handles auth via signInWithGoogle
    return <Landing />;
};

export default Auth;
